module(tests.error-recursive-block-type)
import(prelude)

def( diverge, *a -- *b,
    [ dup run ] dup run)
# mirth-test # merr # 5:11: error: Failed to unify [*x4 !a5 -- *b7] with !a5
# mirth-test # merr # 5:21: error: Failed to unify [...] with !a5
# mirth-test # mret # 1
